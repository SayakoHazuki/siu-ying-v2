export enum DayOfCycle { A, B, C, D, E, F, G, H }
export enum OtherDay { HOLIDAY = "/" }

export enum DayType {
    SCHOOL_DAY,
    HOLIDAY,
}

export enum TimeslotType {
    MorningAssembly,
    Lesson,
    Recess,
    Lunch,
}
